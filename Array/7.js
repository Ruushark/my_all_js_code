
const input=require("readline-sync");

const arr =  input.questionInt("Enter the lenght: ");
const arr1 = input.questionInt("Enter you Range: ");
let index = -1;
for (let i = 0; i<arr; i++){
    if (arr[i]===range) {
        index = [i];
        break
    }
}
console.log(index);






















